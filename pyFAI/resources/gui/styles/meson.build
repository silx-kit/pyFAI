py.install_sources(
   ['gray.json',
    'inferno.json',
    'magma.json',
    'plasma.json',
    'reversed gray.json',
    'viridis.json'],
  pure: false,   # Will be installed next to binaries
  subdir: 'pyFAI/resources/gui/styles'  # Folder relative to site-packages to install to
)

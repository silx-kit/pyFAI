subdir('calibration')
subdir('elements')
subdir('gui')
subdir('openCL')
subdir('sensors')

py.install_sources(
   [
    '__init__.py',
    ],
  pure: false,   # Will be installed next to binaries
  subdir: 'pyFAI/resources'  # Folder relative to site-packages to install to
)
